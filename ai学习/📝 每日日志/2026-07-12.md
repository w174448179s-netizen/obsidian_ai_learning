# 2026-07-12 AI应用开发学习日志
[[AI应用开发学习]]
summary: 系统梳理统一接入网关前置知识体系，从网络基础到模型差异，填充迭代版本文档并更新架构文档

## ✅今日学习内容
- **前置知识体系梳理**（9大领域）：
  - 计算机网络基础：HTTP/SSE协议、RESTful API设计
  - Java Web栈：Spring Boot + WebFlux + WebFilter + Reactor
  - 弹性设计模式：令牌桶限流、熔断器三态、降级阶梯编排
  - 设计模式：适配器模式（核心）、策略模式、模板方法
  - 并发与异步编程：CompletableFuture + 响应式流
  - 认证鉴权：API Key、JWT、OAuth2基础
  - 日志与可观测性：MDC + TraceId + 健康检查
  - 模型厂商差异：OpenAI/Claude/百炼/DeepSeek参数与格式差异
  - 框架工具：Spring AI、Resilience4j、Netty
- **迭代版本文档填充**：`v0.1-统一接入网关.md` 新增完整的前置知识树、实现要点（适配器体系、参数归一化、限流熔断组合、TraceId传递）、测试方案
- **架构文档微调**：`1-接入网关.md` 添加了前置知识链接、迭代版本实现参考、Spring AI 技术基座链接
- **文档沉淀**：沉淀到 [[v0.1-统一接入网关]]，关联 [[1-接入网关]] 和 [[MOC-架构蓝图]]

## ❓疑问待查
- WebFlux 场景下 MDC 自动上下文传播的正确实现方式（Spring Boot 3.2+ ObservationRegistry vs 手动 Hooks）
- 灰度路由中白名单用户与比例灰度的优先级冲突如何处理
- 多模型健康检查的告警通知延迟阈值设置经验值

## 💻代码片段
```java
// 适配器接口 + 参数归一化示例
public interface ModelAdapter {
    CompletableFuture<ModelResponse> chat(ModelRequest request);
    Flux<String> streamChat(ModelRequest request);
    boolean supports(ModelType modelType);
    boolean healthCheck();
}

// OpenAiAdapter 实现参数转换
public class OpenAiAdapter implements ModelAdapter {
    private static final double TEMPERATURE_SCALE = 2.0; // GPT: 0-2
    
    @Override
    public CompletableFuture<ModelResponse> chat(ModelRequest request) {
        var openAiRequest = OpenAIRequest.builder()
            .model(request.getModel())
            .messages(mapMessages(request.getMessages()))
            .temperature(request.getTemperature() * TEMPERATURE_SCALE)  // 归一化转换
            .maxTokens(request.getMaxTokens())
            .build();
        // ... 调用 OpenAI SDK
    }
}
```

## 📋明日计划
- 进入 [[5-AI安全防护]] 模块学习：输入合规检查、防注入策略
- 如果已完成，则进入 [[7-RAG检索增强生成]]

## 🎯AI学习建议
- 今日梳理的前置知识体系覆盖了9大领域，建议在进入下一模块前，先挑一个薄弱环节补强（如 WebFlux 响应式编程不熟的可以先练 Reactor 基础）
- v0.1 版本的实现要点已经可以支撑一个最小可行实现，建议后续实际编码时按"非流式→流式→降级→灰度"的顺序逐步迭代
\n\n

## 🗓️ v0.1 学习计划重构（下午）

**问题诊断：** 原来的 [[v0.1-统一接入网关]] 堆满了前置知识体系（HTTP/SSE/WebFlux/设计模式），但**没有回答"为什么需要这些能力"和"每天学什么、产出什么"**。

**重构方案：** 从"知识清单" → **"能力驱动"**。定义了 10 项能力的 P0-P3 优先级矩阵，每项都回答了"为什么要实现"和"没有它会怎样"。

### 6天学习计划概览

| 天 | 能力 | 产出 |
|----|------|------|
| Day 1 | 协议转换 + 非流式调用 | `/chat` 端点，跑通第一个模型 |
| Day 2 | 流式 SSE | `/chat/stream` 打字机效果 |
| Day 3 | 模型路由 | 精确+灰度路由 |
| Day 4 | 限流 + 熔断 | 3层编排（限流→熔断→调用） |
| Day 5 | 认证鉴权 + TraceId | 安全+可观测的入口 |
| Day 6 | 降级策略 + 整合MVP | 完整可运行网关 |

### 同步更新的文件
- [[v0.1-统一接入网关]] — 重写为能力驱动+按日计划
- [[1-接入网关]] — 补充学习计划链接
- [[MOC-版本迭代]] — 补充能力描述
- 🏠 Home — 更新当前版本描述
- [[AI应用开发学习]] — 脑图同步版本迭代节点
