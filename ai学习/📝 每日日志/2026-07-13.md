# 2026-07-13 AI应用开发学习日志

## ✅今日学习内容
- **AI办公助手主链路架构文档**：完整梳理了API网关、Agent主服务、模型集群的分层职责和核心规则
- **网关核心规则**：主线同协议路由纯透传、异构备用路由独立、X-Logic-Model-Id Header约定、Resilience4j熔断配置
- **Agent主服务五层架构**：输入预处理→意图粗识别→会话记忆→业务编排→模型调用
- **鉴权链路**：SSO JWT登录→网关校验→Redis配额校验→转发至Agent服务

## 📋核心规则总结
1. 主线长会话：全程单一OpenAI兼容modelId，同协议集群熔断降级
2. 参数适配：仅异构备用路由做一次性首包适配
3. 会话隔离：主线RAG/SQL长对话 ≠ 异构纯问答备用会话
4. 性能保障：主干链路无全量JSON反复序列化
5. 可观测性：全链路trace埋点、模型调用日志、token用量统计

## 💻代码片段
```java
// 主线会话：纯标准OpenAI参数，无厂商私有参数
OpenAiChatOptions options = OpenAiChatOptions.builder()
        .model("qwen-openai")
        .temperature(0.7)
        .maxTokens(2000)
        .build();

ChatClient chatClient = ChatClient.builder(openAiChatModel)
        .defaultOptions(options)
        .build();
```

## 🔗明日计划
- 进入 [[3-流量控制]] 模块学习：Rate Limit、Concurrent Limit、Quota、Circuit Breaker