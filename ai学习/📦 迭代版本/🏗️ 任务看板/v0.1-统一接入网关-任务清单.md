---
title: v0.1-统一接入网关 任务清单
tags: [tasks, gateway, v0.1, in-progress]
created: 2026-07-12
deadline: 2026-07-18
status: in-progress
---

# ✅ v0.1-统一接入网关 — 动手任务清单

> 来源：[[v0.1-统一接入网关]] | 预计完成：2026-07-18 | 总任务：**45项** | 已完成：**2项**

---

## 📅 Day 1 — 协议转换（适配器 + 非流式调用）

### 统一模型定义
- [x] 定义 `ModelRequest` / `ModelResponse` 统一模型 `2026-07-12`
- [x] 定义请求/响应的错误码枚举 ✅ 2026-07-12

### ModelAdapter 接口
- [ ] 定义 `ModelAdapter` 接口，包含 `chat()` + `supports()` + `healthCheck()`
- [ ] 实现 `OpenAiAdapter`（非流式，调用 OpenAI SDK）
- [ ] 实现 `ClaudeAdapter`（非流式，调用 Anthropic SDK）
- [ ] 实现 `BailianAdapter`（非流式，调用阿里云百炼 SDK）

### 参数归一化
- [ ] 实现 `ParameterNormalizer` 参数归一化工具类
- [ ] 建立 temperature 0-1 到各模型实际范围的双向映射表
- [ ] 实现 `top_p`、`max_tokens` 等通用参数的归一化

### 非流式端点
- [ ] 暴露 `POST /chat` 端点，接收统一请求，调用对应模型
- [ ] 实现 `CompletableFuture` 异步非流式调用

### 单元测试
- [ ] 参数映射正确性测试
- [ ] 适配器加载测试（多模型实例可以正确注册）

---

## 📅 Day 2 — 流式 SSE（打字机效果）

### ModelAdapter 流式扩展
- [ ] 在 `ModelAdapter` 中添加 `streamChat()` 返回 `Flux<String>`
- [ ] 实现 `OpenAiAdapter.streamChat()`（OpenAI 流式 SDK → Reactor Flux）
- [ ] 实现 `ClaudeAdapter.streamChat()`
- [ ] 实现 `BailianAdapter.streamChat()`

### SSE 端点
- [ ] 创建 `POST /chat/stream` SSE 端点，`MediaType.TEXT_EVENT_STREAM`
- [ ] 实现逐 token 推送（打字机效果）
- [ ] 实现心跳保活机制（防止 HTTP 连接被中间件关闭）

### 响应式基建
- [ ] 流式超时配置（`timeout()` + `onErrorResume()` 兜底）
- [ ] 流式异常处理（断连优雅释放资源）

---

## 📅 Day 3 — 模型路由（灵活选择模型）

### 路由核心
- [ ] 定义 `ModelRouter` 接口，包含 `route()` + `routeWithGray()` + `markUnhealthy()`
- [ ] 实现精确匹配路由（model name → ModelAdapter）
- [ ] 实现默认降级路由（找不到模型时回退）
- [ ] 实现路由表（YAML 静态配置加载）
- [ ] 动态路由表支持（配置中心热刷新，可选）

### 灰度路由
- [ ] 实现白名单灰度（请求头 `X-User-Id` 匹配）
- [ ] 实现百分比灰度（按用户 ID 哈希分桶）
- [ ] 灰度路由 + 熔断联动（模型被标记 unhealthy 后自动跳过）

### 按能力路由
- [ ] 实现能力标签映射（多模态→多模态模型，推理→深度推理模型）
- [ ] 请求参数带能力声明，路由自动匹配具备该能力的模型

### 测试
- [ ] 灰度比例精准度验证（单元测试 + 统计）

---

## 📅 Day 4 — 限流 + 熔断（流量防护）

### 限流
- [x] 配置 Resilience4j CircuitBreaker（按模型实例） `2026-07-12`
- [ ] 配置 Resilience4j RateLimiter（全局限流）
- [ ] 配置按用户/API Key 的独立限流
- [ ] 限流触发时返回 429 响应

### 熔断
- [ ] 熔断器三态测试：关闭 → 开启（失败阈值）→ 半开（试探恢复）
- [ ] 熔断事件监听（日志输出 + 可扩展告警）
- [ ] 熔断触发时返回 503 响应

### 编排
- [ ] 实现限流-熔断-调用的三层编排（`rateLimiter → circuitBreaker → modelAdapter`）
- [ ] WebFlux + Resilience4j Reactor 适配器集成
- [ ] 路由 + 熔断联动集成（熔断后路由跳过坏模型）

### 测试
- [ ] 限流速率验证（固定时间窗口桶消耗正确）
- [ ] 熔断器状态转换测试
- [ ] 限流触发 429、熔断触发 503

---

## 📅 Day 5 — 认证鉴权 + TraceId（安全可观测入口）

### WebFilter 链
- [ ] 实现 `TraceIdFilter`：接收上游 TraceId 或生成新 ID，放入 MDC + 响应头
- [ ] 实现 `AuthFilter`：从 `Authorization: Bearer <key>` 提取 API Key 并校验
- [ ] 实现请求/响应日志格式：`[traceId] path method status duration`
- [ ] 配置 WebFilter 链顺序：`TraceIdFilter → AuthFilter → SizeLimitFilter → Controller`

### 鉴权
- [ ] API Key 存储与校验逻辑（内存 Map / 配置文件起步）
- [ ] 无 API Key → 401，无效 API Key → 403
- [ ] 按 API Key 绑定模型访问权限（可选）

### TraceId 全链路
- [ ] 响应式 MDC 传播配置（`Hooks.enableAutomaticContextPropagation()`）
- [ ] TraceId 在日志中完整贯穿
- [ ] TraceId 在响应头 `X-Trace-Id` 中回传

### 测试
- [ ] 带/不带 API Key 的鉴权行为测试
- [ ] TraceId 贯穿响应头验证

---

## 📅 Day 6 — 降级策略 + 辅助能力 + MVP 整合

### 降级策略
- [ ] 定义降级接口 + 策略链
- [ ] 实现主模型→备模型降级（模型不可用时自动切换）
- [ ] 实现缓存降级（基于语义相似的问答缓存复用）
- [ ] 实现简化模型降级（完整回答 → 纯文本 → 关键词）
- [ ] 实现排队降级（削峰填谷，异步处理返回排队中提示）

### 健康检查
- [ ] 实现定时探活（`@Scheduled` 每 30s 调用 `healthCheck()`）
- [ ] 探活结果缓存到内存（Map<String, HealthStatus>）
- [ ] 健康状态暴露端点（可选 `GET /health`）

### 辅助配置
- [ ] 请求大小限制 Filter（限制 Body 不超过 1MB）
- [ ] 各模型独立超时配置（`ModelTimeoutConfig`）
- [ ] 配置外化（application.yml / 配置中心）

### MVP 整合
- [ ] 所有模块联合集成测试（端到端）
- [ ] 正常流：鉴权 → 路由 → 限流 → 熔断 → 协议转换 → 流式推送
- [ ] 故障流：模型不可用 → 熔断开启 → 路由自动切换 → 降级响应
- [ ] 超限流：并发超限 → 429 响应
- [ ] 写 README / 启动文档

---

## 📋 汇总进度

| 模块 | 总任务 | 已完成 | 进度 |
|------|--------|--------|------|
| Day 1 — 协议转换 | 12 | 1 | █░░░░░░░░░ |
| Day 2 — 流式 SSE | 8 | 0 | ░░░░░░░░░░ |
| Day 3 — 模型路由 | 10 | 0 | ░░░░░░░░░░ |
| Day 4 — 限流熔断 | 9 | 1 | █░░░░░░░░░ |
| Day 5 — 认证 TraceId | 10 | 0 | ░░░░░░░░░░ |
| Day 6 — 降级+整合 | 13 | 0 | ░░░░░░░░░░ |
| **总计** | **62** | **2** | **3%** |

---

[followups heading="下一步"]
- 打开 [[v0.1-统一接入网关]] 对照学习计划逐日推进
- 每完成一天的任务，回到这份清单打勾
- 有新的子任务随时追加，保持清单就是当时的"真相"
[/followups]
